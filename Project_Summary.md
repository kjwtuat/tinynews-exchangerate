# tinynews-exchangerate (AI 대화형 환율 데이터 파이프라인 & 뷰어)

본 문서는 `06. tinynews-exchangerate` 프로젝트의 전체 개발 내용, 작동 원리, 그리고 기술적 특징을 상세히 정리한 문서입니다.

## 1. 프로젝트 개요
- **목적**: 매일 실시간 환율 정보(달러, 엔화, 유로화, 위안화, 파운드화)와 최신 외환 관련 뉴스를 수집하고, 음성 비서(TTS)가 읽기 편한 구어체 문장으로 **AI(Gemini 2.5 Flash)가 즉시 가공하여 저장**하는 서버리스 데이터 파이프라인 구축 및 이를 웹에서 확인하는 파이낸셜 리더 대시보드 제공.
- **아키텍처**: Static API (GitOps 기반의 서버리스 DB)
- **핵심 패키지**: `rss-parser` (구글 뉴스 수집), `@google/generative-ai` (Gemini API 통신), `vite`

## 2. 핵심 기능 및 특징

### 2.1. AI 기반 TTS 최적화 환율 대본 생성 (`fetchExchangeRate.js`)
- `open.er-api.com` 오픈 환율 API를 활용해 USD, JPY(100엔), EUR, CNY, GBP 매매기준율을 수집합니다.
- 구글 뉴스 RSS에서 '원달러 환율' 키워드로 최신 금융 기사 8개를 추출합니다.
- Gemini 2.5 Flash 모델에 수치 및 기사를 입력하여 다음 항목으로 구성된 JSON 대본을 반환받습니다.
  - `speakableTitle`: 핵심 환율 흐름 한 줄 요약
  - `rates`: 주요 통화별 수치, 변동 방향, 변동 폭
  - `script`:
    1. 시장 전체 동향 요약 대본
    2. 통화별 세부 동향 대본
    3. 환율 영향 주요 이슈 대본
    4. 오늘의 외환/환전 팁 대본

### 2.2. 용량 최적화 (7일 자동 보관 로직)
- `public/data/` 폴더 내 생성된 지 7일이 지난 `.json` 파일들을 자동으로 삭제(unlink)하여 Git 저장소 용량을 최적화합니다.
- 보관 중인 날짜 리스트는 `index.json`으로 업데이트됩니다.

### 2.3. 파이낸셜 대시보드 UI (Frontend Viewer)
- **디자인 테마**: 다크 파이낸셜 글래스모피즘 테마 (Inter + Pretendard 폰트).
- **작동 방식**: 접속 시 `index.json`을 읽어 날짜 선택 옵션을 구성하고, 비동기(Fetch)로 선택된 날짜의 환율 카드 및 대본을 렌더링합니다.
- **음성 읽기 기능 (TTS)**: Web Speech API를 탑재하여 전체 대본 연속 재생 및 단락별 읽기 기능을 제공합니다.

## 3. 로컬 실행 가이드
- **의존성 설치**: `npm install`
- **수집 파이프라인 수동 실행**: `npm run fetch` (오늘 날짜의 환율/뉴스 데이터를 AI로 가공해 `public/data/`에 저장합니다.)
- **웹 뷰어 실행**: `npm run dev`
